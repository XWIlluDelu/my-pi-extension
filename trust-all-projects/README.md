# trust-all-projects

Pi extension that accepts every `project_trust` prompt and remembers the decision.

Linked into Pi from:

```text
~/.pi/agent/extensions/trust-all-projects -> ~/.pi/custom-extensions/trust-all-projects
```
